<h1>Lista de Carpetas</h1>

<p><%= link_to 'Nueva Carpeta', new_carpeta_path %></p>

<%= will_paginate @carpetas %>
<table border="1">
  <tr>
    <th>Fecha</th>
    <th>Orden Servicio</th>
    <th>Nro Carpeta</th>
    <th>Liquidador</th>
    <th>Clientes</th>
  </tr>

<% @carpetas.each do |carpeta| %>
  <tr>
    <td><%= lo carpeta.fecha %></td>
    <td><%= carpeta.orden_servicio %></td>
    <td><%= carpeta.nro_carpeta %></td>
    <td><%= carpeta.personal %></td>
    <td><%= carpeta.cliente %></td>
    <td>
      <%= link_to("ver", carpeta ) %>
      <%= link_to("editar", edit_carpeta_path(carpeta) ) %>
      <%= link_to("borrar", carpeta, :method => :delete, :confirm => 'Esta seguro?' ) %>
    </td>
  </tr>
<% end %>
</table>
